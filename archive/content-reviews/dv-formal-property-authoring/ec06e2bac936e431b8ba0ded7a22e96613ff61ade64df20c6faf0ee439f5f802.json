{
 "slug": "dv-formal-property-authoring",
 "wave": "b3",
 "gate": "author -> lint 1.000 -> adversarial review -> fix -> independent recheck",
 "recorded_at": "2026-08-05T19:53:59Z",
 "author": {
  "agent": "author:dv-formal-property-authoring",
  "lint_line": "",
  "uncertainties": []
 },
 "review": {
  "agent": "review:dv-formal-property-authoring",
  "must_fix": [
   "C005 (warn, confirmed): handoff field 'points at' offers the value 'over-constraint', but no step ever tells the reader to assign it",
   "C005 (warn, confirmed): handoff field 'points at' offers the value 'spec-gap', but no step ever tells the reader to assign it",
   "C005 (warn, confirmed): handoff field 'points at' offers the value 'undecided', but no step ever tells the reader to assign it",
   "C005 (warn, confirmed): handoff field 'points at' offers the value 'under-constraint', but no step ever tells the reader to assign it",
   "Beyond the 4 confirmed values: the entire `points at` field (line 285-286) has no decision table anywhere in the procedure — unlike step 4's clean directive table (lines 154-159), no step says 'set points at: X' for any of its 6 values. Even the two values that dodge the mechanical checker ('design', 'property') survive only because those literal words appear elsewhere in unrelated prose, not because a rule assigns them: the cex bullet (line 252-254) describes exactly the 'under-constraint' scenario (a missing assume) without ever naming that value, and the unreachable bullet (line 258-259) describes exactly the 'design or spec-gap' scenario without ever naming those values either. Add an explicit mapping table from proof-result classification to `points at`, the way step 4 maps obligation type to directive.",
   "Rename `proof result` (lines 244, 283) to the registered `proof status` and adopt its canonical spellings (`proven | falsified | bounded | inconclusive | vacuous | not-read`) per _shared/handoff-vocabulary.md and the precedent already set by dv-formal-apps and dv-formal-overconstraint-credit — see technical_errors for detail.",
   "Reconcile step 5's Grep count with its own per-assume framing, or explicitly cap it (e.g. 'one spec-clause Grep and one duplicate-check Grep per new assume, up to N') so the stated 'four Greps, each named' budget in the retrieval-budget section is actually achievable on a multi-obligation property set — see budget_violations for detail."
  ],
  "technical_errors": [
   "High confidence (~90): lines 244 and 283 define a field `proof result` with values `proved | bounded-pass | cex | vacuous | unreachable | undetermined`. This is the same underlying concept as the T1-REGISTERED field `proof status` (_shared/handoff-vocabulary.md lines 55, 61-66: `proven | falsified | bounded | inconclusive | vacuous | not-read`), which sibling skills dv-formal-apps (line 119-120, which explicitly instructs authors to 'record each property in the canonical `proof status` spelling') and dv-formal-overconstraint-credit (line 279) both already follow. 5 of 6 tokens here are respelled synonyms of the canonical set (proved/proven, cex/falsified, bounded-pass/bounded, undetermined/inconclusive); only `vacuous` matches exactly. This is precisely the 'unregistered field name colliding with another skill's meaning for the same word' failure standing rule 10 warns about, and it evades the pack's own C008 synonym checker only because the >=2-shared-token threshold isn't met (only 1 token, `vacuous`, is shared verbatim). Fix: rename to `proof status` and use the canonical spellings; if cover-reachability (`unreachable`) is a genuinely new axis, either drop it (the block already carries a `witness` field at line 282 that records whether the cover was hit, making `unreachable` largely redundant within the same record) or register the extension in _shared/handoff-vocabulary.md with sign-off, per the file's own governance rule ('registration commits all 83 skills... carries the same sign-off as publishing').",
   "Moderate-high confidence (~80): the skill's central assume-justification diagnostic — 'a cover that was reachable before and unreachable after names the assume that killed it' (lines 194-196) and 'a cover that was unreachable before any assume existed is a finding about the design or the spec' (Gotchas, line 326-327) — requires comparing two different formal runs (with vs. without a given assume). The Fill table's Proof report slot (line 63) and the retrieval budget (item 3, lines 90-93) only provision for a single per-property status report, read once via one Grep in step 7. No step, slot, or budget line describes obtaining or reading a prior/baseline report, so the procedure as written cannot actually execute the before/after comparison it repeatedly relies on as its main review mechanism for judging whether an assume is safe."
  ],
  "verb_honesty": [
   "Clean. Every agent-performed step is Read/Grep/Glob or a drafting/classifying verb. Both places that need an engine are correctly handed off: step 7 line 239-240 \"The agent cannot start an engine and must not invent what one would have printed. Ask the engineer to run our formal tool over this property set and give you the path to the per-property status report\"; the intro (lines 29-31) also states the read-only boundary plainly. No bare Run/Compute/Merge verb assigned to the agent anywhere."
  ],
  "hallucination_risks": [
   "None found at high confidence. The skill is disciplined about deferring tool-specific facts to [[FILL:]] slots or explicit 'ask' language (e.g. line 255-257 on vacuity reporting: 'What a tool reports as vacuity, and whether that reporting is on by default, is a tool fact — ask rather than assume'). No vendor names, invented flags, or invented message strings were found outside the SVA/IEEE-1800 standard syntax used in the step 6 template (lines 206-213), which matches the same generic template already used in the golden-adjacent dv-protocol-checker-rule (lines 153-160)."
  ],
  "budget_violations": [
   "High confidence (~85): budget item 3 (lines 90-93) states step 5 spends exactly two Greps total ('Two in step 5 — one in the spec-extract file for the clause behind an assume, one in the existing property file for an assume already declared there'), and the running total is fixed at 'Four Greps, each named.' But the step 5 procedure text frames both Greps as per-assume actions: 'Grep the spec-extract file for the clause to confirm it says what you remember; that is the first of step 5's two Greps' (line 178-179, attached to every assume using spec-clause justification) and 'Before adding a new assume, Grep the existing property file for the signal name; that is the second Grep' (line 189-191, explicitly 'before adding A NEW assume'). Given the obligation cap allows up to ~12 obligations (budget item 6, line 99-101) and several will plausibly become assumes each needing its own clause-confirmation and duplicate-check Grep, the real Grep count scales with the number of assumes drafted, not a fixed 2 — the stated 4-Grep-total budget only holds for a property set with at most one assume, which contradicts the skill's own multi-obligation framing."
  ],
  "open_twice": "\"Mostly yes on a first read — the Gotchas section (lines 312-343) is concrete and technically sound (the assume-on-design-driven-signal lobotomy, the reset-polarity all-covers-unreachable tell, the preponed-region off-by-one), the handoffs are honest, and the retrieval budget mostly reconciles with what the steps actually spend. But a reviewer who cross-references this skill's report block against dv-formal-apps' or dv-formal-overconstraint-credit's (which the pack explicitly designed to be comparable via the registered proof-status vocabulary) would hit a silent field-name/value mismatch that looks like a different concept when it is not — and an engineer with more than one or two assumes would discover the stated four-Grep budget is quietly false. Both are exactly the kind of trust-eroding inconsistency that makes a busy engineer stop trusting the rest of the document.\""
 },
 "fix": {
  "agent": "fix:dv-formal-property-authoring",
  "fixed": [
   "C005 x4 (points at: over-constraint / under-constraint / spec-gap / undecided) — closed by ADDING the branches, not by mentioning the words. Step 7 now carries an explicit 9-row decision table mapping each proof-status outcome (plus the baseline-run comparison) to a `points at` value, in the same shape as step 4's directive table: legal cex -> design; illegal cex -> under-constraint; vacuous / dead antecedent cover -> property; cover the baseline hit but the constrained run cannot -> over-constraint; cover neither run hits and the spec says the behaviour is legal -> design, and where the spec does not settle it -> spec-gap; inconclusive or bounded short of the Depth policy -> undecided; proven / bounded-at-depth / not-read -> leave empty.",
   "must_fix / technical_errors: renamed `proof result` to the T1-REGISTERED `proof status` and adopted its canonical spellings verbatim (`proven | falsified | bounded | inconclusive | vacuous | not-read`), matching dv-formal-apps and dv-formal-overconstraint-credit. Every bullet, the step-8 block and all prose were re-spelled; no `proved`/`bounded-pass`/`cex`/`undetermined` value tokens remain. The full canonical set is emitted, so no T2 narrowing row is needed in _shared/ (which I must not edit) and C007 does not fire.",
   "The dropped `unreachable` axis was NOT silently lost: step 7 states the cover mapping explicitly (`proven` = cover hit / witness trace, `falsified` = cover proved unhittable, `bounded` = not hit inside the depth), and the `witness` field now records whether the baseline run hit the cover too. This was the redundancy the reviewer identified, resolved by mapping rather than deletion.",
   "technical_errors (assume-justification diagnostic had no baseline): step 7's handoff now asks the engineer for TWO status reports — one with the step 5 assumes in force, one with them disabled — and Greps each once. The Proof report slot was widened to ask whether one file is kept per run and how two runs are told apart on disk; the profile's **Run identity** fact (referenced, not re-asked, per rule 8) names them apart. Step 5 no longer claims to judge the pairing: it records the pairing as unjudged and defers the before/after comparison to step 7. If only one report comes back, the skill says so, forbids `points at: over-constraint`, and the set-level `survives` line records `unjudged: one report only`.",
   "budget_violations (step 5's Greps scaled with the number of assumes): both step 5 Greps are now batched over the whole draft and cannot scale. The spec-clause check collects every clause reference first and Greps the spec extract ONCE with them alternated; the duplicate check Greps the property file ONCE for the `assume` keyword and matches signal names against that one result by eye. Budget item 3 was rewritten to `Five Greps, each named, and not one of them per-obligation` (1 in step 2, 2 in step 5, 2 in step 7) and states the rule that keeps it true at twelve obligations. Reads stay at five: item 4 now spends the two 40-line windows across both reports. Stopping rule 7 forbids a third Grep hunting for a missing baseline.",
   "verb_honesty (was already clean — preserved and extended): the new two-run request uses the same explicit handoff phrasing (`Ask the engineer to run our formal tool ... and to give you the path to each per-property status report`). No new agent-performed engine verb was introduced; every added step is Grep, bounded Read, classify or draft.",
   "Also closed the C005 warns the registry-less single-skill run raised on `window` (same-cycle / next-cycle / within-n / bounded-eventually) by adding a real step-6 decision table mapping the clause's wording to the window value and to the SVA shape it wants — so command 1 prints zero findings of any level, not just a clean per-skill line.",
   "Unprompted honesty fix: the closing paragraph of `When to use something else` claimed there is no pack skill for making a stubborn proof converge. `dv-formal-convergence` now exists and covers exactly that, so the paragraph routes there and step 7's `inconclusive` bullet names it. Leaving it would have been a false routing claim of the kind rule 2 exists to prevent.",
   "Metadata: semiskill-version 1.0.0 -> 1.1.0; semiskill-review-by staggered 2027-10-08 -> 2027-06-17 (no sibling uses it; the old date was also just past the 14-month ceiling). Role and level untouched at formal-verification / intermediate."
  ],
  "not_fixed": [
   "Nothing in must_fix, technical_errors, verb_honesty or budget_violations was left open. Two deliberate scoping notes, neither of which leaves a review claim standing: (1) I did not register a `proof status` narrowing row in _shared/handoff-vocabulary.md — instead the skill emits the FULL canonical six-value set, which needs no registration and touches no file another agent owns, per the instruction to edit only my own directory. (2) The `unreachable` token was not registered as a new canonical value for the same reason; it is expressed through the documented cover mapping plus the existing `witness` field, which is the resolution the review itself recommended."
  ],
  "lint_line": "skills\\dv-formal-property-authoring\\SKILL.md  [approve 1.000]  (stage 3 not evaluated - advisory only) / clean / 1/1 clean - 0 errors - 0 advisories - 1 predicted to publish"
 },
 "recheck": {
  "agent": "recheck:dv-formal-property-authoring",
  "ready": false,
  "why": "Lint is 1.000 and clean, the pack-wide consistency check returns zero findings for this slug, metadata role/level are exactly formal-verification/intermediate, all nine slots are spent, and the 2-Glob/5-Grep/5-Read budget accounts for every Grep the steps make. The SVA domain content is accurate and unusually sharp. But three defects would produce a wrong artifact or a needless interruption on first use: (1) the description advertises 'a formal run proves everything and you suspect over-constraint' and 'covers went unreachable after you added a constraint', which are dv-formal-overconstraint-credit's headline triggers, yet that sibling is never named in 'When to use something else' — it appears once at line 337 only as a field-spelling note, while step 7 builds a home-grown two-run substitute for it; (2) 'our citation form' is a house convention consumed at lines 186 and 342 with no [[FILL:]] slot, against the pack's own precedent in dv-protocol-checker-rule, and line 362's spec revision has no source slot either; (3) step 7 Greps both reports and says 'Classify each row into proof status' while the record carries one proof status and one points at, never stating that assert statuses come from the constrained run — applied literally to the baseline, every falsified assert has a trace the environment cannot legally produce and table row 2 marks the whole set points at: under-constraint.",
  "remaining": [
   "Routing: 'When to use something else' never names dv-formal-overconstraint-credit, although the description's 2nd and 4th triggers ('a formal run proves everything on the first attempt and you suspect over-constraint', 'covers went unreachable after you added a constraint') are that skill's headline triggers, and step 7's bounded/Depth-policy handling duplicates it. It is mentioned only at line 337 as a proof-status spelling note.",
   "Unslotted house convention: line 185-186 'with the document, revision and clause reference in our citation form' and line 342 'spec ref : <document name, revision and clause, in our citation form>' — no [[FILL:]] slot asks for the citation form. dv-protocol-checker-rule slots exactly this ('the citation form our reviews expect for a clause'). Line 362 '<the spec revision drafted against>' likewise has no source slot.",
   "Step 7 ambiguity: 'One Grep of each report ... Classify each row into proof status' covers both runs, but the per-property record has one proof status and one points at, and no sentence says assert/vacuity statuses come from the constrained run while the baseline is consulted only for covers. Literal application to the baseline marks every assert points at: under-constraint.",
   "Line 224 template '@(<the clock from the slot>)' drops the active clock edge; the Clock and reset slot asks for the reset's polarity but never the clock's edge, so literal substitution gives @(clk), a both-edge clocking event.",
   "The Clock and reset slot asks 'how long the environment holds it' and no step consumes the reset hold duration — including the $past guard bullet, which talks about a reset-released counter without referring back to the slot.",
   "Budget rule 5 ('anchor it on a leading input, output or the property-id prefix before reading anything') implies a sixth Grep against a ledger that states 'Five is the whole ledger', and none of the five declared Greps searches for ports, so the input/output anchors correspond to no Grep the procedure makes.",
   "The Proof report slot asks 'how two runs are told apart on disk' while line 70 says the profile's Run identity 'is what tells the two apart' — the same job asked twice, with no statement of how they differ, in a file that otherwise declares its narrowing against Log location explicitly.",
   "Line 327 'Then set class for the run as a whole' places a run-level value inside the one-record-per-property block.",
   "The step 6 code template hardcodes |=> immediately above a window table stating the operator must follow the window; a reader who copies the block gets the non-overlapping form for a same-cycle clause.",
   "Domain voice: 'Never assert something only the environment drives. That is a check on the testbench, and it will fail the moment somebody legitimately changes the stimulus.' In formal there is no stimulus — free inputs falsify it on the first run; the rule is right, the stated reason is the simulation one.",
   "442 lines against the stated 180-260 target (siblings dv-formal-apps and dv-formal-overconstraint-credit are 423-424, so this is pack-consistent rather than an outlier — informational)."
  ],
  "new_problems": [
   "Step 7's two-run mechanism reads as the newest layer and carries the one unresolved contract in the file: which of the two reports feeds the single proof status and points at fields. The 'if only one report comes back' paragraph implies the constrained run, but 'Classify each row into proof status' after 'One Grep of each report' contradicts it.",
   "The same two-run mechanism is what makes the missing dv-formal-overconstraint-credit route conspicuous — this skill now performs a weaker version of that staff-level skill's audit (baseline comparison, bounded-depth policy check) without telling the reader the dedicated skill exists."
  ]
 }
}