# Copy to .env (gitignored) and adjust as needed.
# Local Docker Postgres 16 — ADR-005 (egress-controlled artifact store + pipeline).
DATABASE_URL=postgresql://semiskill:semiskill@localhost:5432/semiskill
